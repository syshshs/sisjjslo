# libXray Android AAR

This directory is intentionally kept free of generated native binaries in the source archive.

Generate the official XTLS/libXray Android AAR with:

```bash
./scripts/build-libxray-aar.sh
```

The script places `libXray.aar` here. Then build the Android app normally.
